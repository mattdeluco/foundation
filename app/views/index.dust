{>"layouts/default" /}

{<title}
    {site.title} ({site.env})
{/title}

{<body}
<div class="container">
  <div class="jumbotron text-center">
    <h1>{site.title}</h1>
    <h2>{site.tagline}</h2>
    <h3>{site.env}</h3>
    <a href="/user/new" class="btn btn-g btn-success">Sign up!</a>
  </div>
</div>
{/body}